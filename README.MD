# My Flask Site

Простой и элегантный веб-сайт, построенный на Flask с использованием современных веб-технологий.

## 🚀 Возможности

- **Адаптивный дизайн** - корректное отображение на всех устройствах
- **Темная/светлая тема** - переключение между темами в один клик
- **Интерактивные элементы** - анимации и динамические уведомления
- **Многостраничность** - главная, о нас, контакты
- **Контактная форма** - с валидацией и обратной связью
- **Мобильное меню** - удобная навигация на мобильных устройствах

## 🛠 Технологии

- **Backend**: Python Flask
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **Стилизация**: CSS Grid, Flexbox, CSS анимации
- **Иконки**: Font Awesome 6
- **Шрифты**: Segoe UI (системный стек)

## 📁 Структура проекта

```
my-flask-site/
├── app.py              # Основное Flask-приложение
├── requirements.txt    # Зависимости Python
├── static/            # Статические файлы
│   ├── css/
│   │   └── style.css  # Основные стили
│   └── js/
│       └── script.js  # Клиентский JavaScript
└── templates/
    └── index.html     # HTML шаблон
```

## ⚙️ Установка и запуск

### 1. Клонирование репозитория
```bash
git clone <ваш-репозиторий>
cd my-flask-site
```

### 2. Создание виртуального окружения (рекомендуется)
```bash
python -m venv venv
```

### 3. Активация виртуального окружения
- **Windows**:
  ```bash
  venv\Scripts\activate
  ```
- **Mac/Linux**:
  ```bash
  source venv/bin/activate
  ```

### 4. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 5. Запуск сервера
```bash
python app.py
```

### 6. Открытие в браузере
Перейдите по адресу: [http://localhost:5000](http://localhost:5000)

## 🌐 Маршруты приложения

- `GET /` - Главная страница
- `GET /about` - Страница "О нас"
- `GET /contact` - Страница "Контакты"

## 🎨 Особенности дизайна

### Цветовая палитра
- Основной градиент: `#6a11cb` → `#2575fc`
- Фон: `#f5f5f5` (светлая), `#1a1a1a` (темная)
- Текст: `#333333` (светлая), `#ffffff` (темная)

### Адаптивные брейкпоинты
- **Desktop**: > 768px
- **Mobile**: ≤ 768px

### Компоненты
- Навигационная панель с градиентом
- Карточки с эффектом поднятия
- Анимированные кнопки
- Слайдер уведомлений
- Адаптивная контактная форма

## 📱 Мобильная версия

На мобильных устройствах:
- Навигация преобразуется в гамбургер-меню
- Карточки располагаются в один столбец
- Уменьшаются отступы и размеры шрифтов
- Оптимизируются все интерактивные элементы

## 🔧 Кастомизация

### Изменение цветов
Отредактируйте файл `static/css/style.css`:
```css
/* Основной градиент */
.navbar {
    background: linear-gradient(135deg, #ВАШ_ЦВЕТ1 0%, #ВАШ_ЦВЕТ2 100%);
}
```

### Добавление новых страниц
1. Добавьте новый маршрут в `app.py`:
```python
@app.route('/новая-страница')
def new_page():
    return render_template('index.html', page='new_page')
```

2. Добавьте соответствующий блок в `templates/index.html`:
```html
{% if page == 'new_page' %}
<section class="page-content">
    <!-- Ваш контент -->
</section>
{% endif %}
```

### Изменение шрифтов
В `static/css/style.css` замените:
```css
body {
    font-family: 'Ваш шрифт', sans-serif;
}
```

## 🧪 Тестирование

Протестируйте функциональность:
1. Нажмите кнопку "Нажми меня" на главной странице
2. Попробуйте отправить форму на странице контактов
3. Переключите тему с помощью кнопки в левом нижнем углу
4. Проверьте адаптивность, изменяя размер окна браузера

## 🐛 Отладка

При возникновении проблем:

1. Проверьте, активировано ли виртуальное окружение
2. Убедитесь, что все зависимости установлены:
   ```bash
   pip list | grep Flask
   ```
3. Проверьте логи Flask в терминале
4. Откройте консоль разработчика в браузере (F12) для проверки JavaScript ошибок

## 📝 Требования

- Python 3.7+
- Flask 2.3.3
- Современный браузер с поддержкой ES6+

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 🙏 Благодарности

- [Flask](https://flask.palletsprojects.com/) - микрофреймворк для Python
- [Font Awesome](https://fontawesome.com/) - иконки
- [Google Fonts](https://fonts.google.com/) - шрифты

## 📞 Поддержка

Для вопросов и предложений:
- Создайте Issue в репозитории
- Отправьте email: email@example.com

---

⭐ Если вам понравился этот проект, поставьте звезду на GitHub!